changelog:
  - type: FIX
    issueLink: https://github.com/solo-io/gloo-mesh-enterprise/issues/17005
    resolvesIssue: false
    description: |
      Ensures the `Type=object` marker is still respected when disabling kubebuilder markers and validations. This ensures recursive fields will still work.
